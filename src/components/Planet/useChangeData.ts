import { PlanetProps } from "@/data/dataType";
import { useEffect, useState } from "react";

type HandleClick = (e: React.MouseEvent<HTMLButtonElement>) => void;

export const useChangeData = (planet: string) => {
}